---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<article class="max-w-none bg-base-100 rounded-2xl p-4 shadow-md">
  <header class="mb-6">
    <div class="text-2xl mb-2 text-primary font-serif">{title}</div>
  </header>
  <slot />
</article>
